@page "/education"
@inject NavigationManager NavigationManager
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SPCA Pet Boarding</title>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;700&display=swap" rel="stylesheet">
    <style>
        html, body {
            font-family: 'Quicksand', sans-serif;
            background-color: white;
            color: #333333;
            margin: 0;
            padding: 0;
        }

        .boarding-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 30px;
            background-color: #143963;
            border-radius: 20px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
        }

        .heading {
            text-align: center;
            font-size: 2.5em;
            color: white;
            margin-bottom: 20px;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.1);
        }

        .card {
            background-color: white;
            border: 1px solid #143963;
            border-radius: 10px;
            padding: 20px;
            margin: 15px 0;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            color: #143963;
        }

            .card:hover {
                transform: scale(1.05);
                box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.3);
                /*  background-color: #143963;
                        color: white; */
            }

            .card h2 {
                font-size: 1.8em;
                color: #143963;
                margin-top: 0;
            }



        .content p {
            font-size: 1em;
            line-height: 1.6;
            color: #333333;
        }

        .rates-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

            .rates-table th, .rates-table td {
                padding: 10px;
                text-align: left;
                border-bottom: 1px solid #143963;
            }

            .rates-table th {
                font-weight: bold;
                color: #143963;
            }

            .rates-table td {
                color: #333333;
            }

        .card:hover .rates-table th,
        .card:hover .rates-table td {
            /*  color: white; */
            /* border-bottom: 1px solid white; */
        }

        .highlight {
            font-weight: bold;
            color: #143963;
        }

        .back-button {
            background-color: #ffffff;
            color: #143963;
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            font-size: 1em;
            cursor: pointer;
            margin-bottom: 20px;
            transition: background-color 0.3s, transform 0.3s;
        }

            .back-button:hover {
                /* background-color: #143963;
                        color: white; */
                transform: scale(1.05);
            }

    </style>
</head>
<body>
    <div class="boarding-container">

        <button class="back-button" @onclick="NavigateBack">Back </button>

        <h1 class="heading">SPCA Education programs</h1>

        <div class="card">
            <h2>Kiddie progams</h2>
            <p>At the SPCA, we provide a platform for kids to learn. Keep a eye on the events tab to see when we open our doors to the public to give your young ones a chance to learn something new about animal care!</p>
        </div>

        <div class="card">
            <h2>Building experience</h2>
            <p>Getting involved with the SPCA through volunteerwork can build up experience for teenagers. Helping out with odd jobs around the premises will prove to be a good educational experience for whoever is willing to lend a hand.</p>
            <p class="highlight">Please note: Volunteers must be 15 years or older</p>
        </div>

        <div class="card">
            <h2>Other information</h2>
            <p>The schedule at the SPCA is subject to change very often so if you are interested in becoming more involved with activities taking place, be sure to contact us!</p>
            
        </div>
    </div>

    @code {
        private void NavigateBack() => NavigationManager.NavigateTo("/information");
    }

</body>
</html>