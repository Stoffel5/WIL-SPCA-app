@page "/pbo"
@inject NavigationManager NavigationManager
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Volunteer Page</title>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;700&display=swap" rel="stylesheet">
    <style>
        html, body {
            font-family: 'Quicksand', sans-serif;
            background-color: white;
            color: #333333;
            margin: 0;
            padding: 0;
        }

        .centered-text {
            text-align: center;
            font-size: 2.5em;
            color: #143963;
            margin-top: 20px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
        }

        .volunteer-container {
            padding: 20px;
            margin: auto;
            max-width: 1200px;
            background-color: #143963;
            border-radius: 20px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
        }

        .volunteer-intro {
            text-align: center;
            margin-bottom: 40px;
            font-size: 1.5em;
            color: white;
            font-weight: bold;
        }

        .volunteer-grid {
           /*  display: grid; */
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
            justify-items: center;
            padding: 10px;
        }

        .volunteer-card {
            background-color: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
            text-align: center;
            padding: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            color: #ffffff;
            min-width: 250px;
            max-width: 300px;
            height: 350px;
            /* display: flex;
            flex-direction: column; */
            justify-content: space-between;
        }

        .volunteer-card:hover {
            transform: scale(1.05);
            box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.3);
                background-color: white;
                
        }

               

               

        .volunteer-card img {
            max-width: 100%;
            height: 180px;
            object-fit: cover;
            border-radius: 10px;
            margin-bottom: 15px;
        }

        .volunteer-card h4 {
            margin: 10px 0;
            font-size: 1.4em;
                color: #143963;
        }

        .volunteer-card p {
            font-size: 1em;
                color: #143963;
            margin-top: auto;
        }

        .main-content {
            padding: 40px;
        }

        .back-button {
            background-color: #ffffff;
            color: #143963;
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            font-size: 1em;
            cursor: pointer;
            margin-bottom: 20px; 
            transition: background-color 0.3s, transform 0.3s;
        }

            .back-button:hover {
                background-color: white;
                color: #143963;
                transform: scale(1.05);
            }

        .sticker-container {
            position: fixed;
            bottom: 10px;
            right: 10px;
            padding: 10px;
        }

        .sticker-image {
            max-width: 120px;
            height: auto;
            border-radius: 15px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
            transition: box-shadow 0.3s ease-in-out;
        }

        .sticker-image:hover {
            box-shadow: 0 0 10px #ffffff, 0 0 20px #ffffff, 0 0 30px #ffffff;
        }
    </style>
</head>
<body>
    <h2 class="centered-text">We are a proud established PBO</h2>

    <div class="main-content">



        <div class="volunteer-container">

            <button class="back-button" @onclick="NavigateBack">Back </button>

            <div class="volunteer-intro">
                <p>The SPCA is a Public Benefit Organisation which means that we are responsible for our own funding in order to take care of the animals</p>
            </div>
            <div class="volunteer-grid">
                <div class="volunteer-card">
                    <img src="images/logo.png" alt="SPCA Logo" class="spca-logo" />
                    <h4>SPCA</h4>
                </div>
            </div>

            <div class="volunteer-intro">
                <p>We do this by means of public donations and various fundraising events. We rely on the generosity and support of animal loving members of the public. </p>
            </div>

            <div class="volunteer-intro">
                <p>We would like you to know that not only will your donation directly benefit the animals in our area but - through a sharing agreement between all SPCA's nationwide - a portion will be used in other, less fortunate areas to benefit animals over a wider area. </p>
            </div>
            <div class="volunteer-intro">
                <p>We thank you for all your support!</p>
            </div>
            
            <div class="sticker-container">
                <img src="stickers/kitty1.png" alt="Cute Kitty Sticker" class="sticker-image" id="stickerImage" />
            </div>
        </div>
    </div>

    @code {
        private void NavigateBack() => NavigationManager.NavigateTo("/information"); 
}

</body>
</html>
