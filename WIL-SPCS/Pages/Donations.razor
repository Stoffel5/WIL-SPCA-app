@page "/donations"
<button class="open-btn" onclick="openNav()">☰</button>

<div id="sideNavbar" class="side-navbar">
    <a href="javascript:void(0)" class="close-btn" onclick="closeNav()">×</a>
    <a href="/adopt" class="nav-link">Adopt a Pet</a>
    <a href="/awareness" class="nav-link">Awareness</a>
    <a href="/donations" class="nav-link">Donations</a>
    <a href="/volunteer" class="nav-link">Volunteer</a>
    <a href="/gallery" class="nav-link">Gallery</a>
    <a href="/boarding" class="nav-link">Animal Boarding</a>
    <a href="/events" class="nav-link">Events</a>
</div>

<div class="main-content">

    <div class="donations-container">
        <h1 class="centered-heading">Support Our SPCA</h1>
        <p class="intro-paragraph">
            The SPCA is always in need of supplies to care for the animals. Currently, we are in urgent need of the following items:
        </p>

        <!-- Gallery-like donation items -->
        <div class="gallery-container">
            <div class="gallery-item">
                <img src="css/blankets.png" alt="Blankets" class="gallery-image">
                <p>Blankets to keep animals warm during cold months</p>
            </div>
            <div class="gallery-item">
                <img src="css/dogfood.png" alt="Dog Food" class="gallery-image">
                <p>Nutritious Dog Food for our hungry canines</p>
            </div>
            <div class="gallery-item">
                <img src="css/catfood.png" alt="Cat Food" class="gallery-image">
                <p>Cat Food to nourish our lovely felines</p>
            </div>
            <div class="gallery-item">
                <img src="css/dogtoys.png" alt="Interactive Toys" class="gallery-image">
                <p>Interactive Toys to keep both dogs and cats entertained</p>
            </div>
            <div class="gallery-item">
                <img src="css/cleaning.png" alt="Cleaning Supplies" class="gallery-image">
                <p>Cleaning Supplies to maintain a hygienic and safe environment</p>
            </div>
        </div>

        <p class="intro-paragraph">
            Your generous donations help us provide a safe and comfortable environment for the animals. Thank you for your support!
        </p>

        <!-- Donation Progress Bar -->
        <div class="progress-container">
            <h2 class="progress-heading">Donation Progress</h2>
            <div class="progress-bar">
                <div class="progress-fill"></div>
            </div>
            <p class="progress-text">Goal: R10,000 | Raised: R5,000</p>
        </div>
    </div>

</div>

<style>
    /* Font family */
    body {
        font-family: 'Quicksand', sans-serif;
    }

    /* Centering and layout improvements */
    .main-content {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        min-height: 100vh;
        background-color: #143963;
        padding: 20px;
    }

    .donations-container {
        padding: 20px;
        text-align: center;
        background-color: #143963;
        border-radius: 20px;
        box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.2);
        max-width: 1200px;
        margin: auto;
    }

    .centered-heading {
        font-size: 2.5em;
        color: #ffffff;
        margin-bottom: 20px;
    }

    .intro-paragraph {
        font-size: 1.2em;
        color: #ffffff;
        margin-bottom: 20px;
    }

    .gallery-container {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
        gap: 20px;
        justify-items: center;
        padding: 10px;
    }

    .gallery-item {
        background-color: #4775A9;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
        text-align: center;
        padding: 20px;
        transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
        position: relative;
    }

        /* Hover effect on donation items */
        .gallery-item:hover {
            transform: scale(1.05);
            box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.5);
            background-color: #ffffff;
        }

    .gallery-image {
        max-width: 100%;
        height: auto;
        border-radius: 10px;
        margin-bottom: 15px;
        object-fit: cover;
    }

    .gallery-item p {
        margin: 10px 0;
        font-size: 1em;
        color: #333;
    }

    /* Progress Bar Styles */
    .progress-container {
        margin-top: 30px;
        padding: 20px;
        color: white;
        border-radius: 8px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    .progress-heading {
        font-size: 2em;
        margin-bottom: 20px;
    }

    .progress-bar {
        width: 100%;
        height: 25px;
        background-color: #e0e0e0;
        border-radius: 12px;
        overflow: hidden;
        margin-bottom: 10px;
        position: relative;
        transition: box-shadow 0.3s ease;
    }

    .progress-fill {
        height: 100%;
        background-color: #28a745;
        border-radius: 12px 0 0 12px;
        width: 0;
        transition: width 1s ease;
    }

    .progress-bar:hover .progress-fill {
        width: 50%;
    }

    .progress-fill.loaded {
        width: 50%;
    }

    .progress-text {
        font-size: 1.2em;
    }

    /* Side Navbar */
    .side-navbar {
        height: 100%;
        width: 0;
        position: fixed;
        top: 0;
        left: 0;
        background-color: #143963;
        overflow-x: hidden;
        transition: 0.5s;
        padding-top: 60px;
        z-index: 100;
        border-right: 2px solid #ffffff;
    }

        .side-navbar .nav-link {
            padding: 15px 20px;
            font-size: 1.2em;
            color: #e0f7fa;
            display: block;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
            text-decoration: none;
        }

            .side-navbar .nav-link:hover {
                background-color: #4775A9;
                color: #ffffff;
                box-shadow: 0 0 10px #ffffff;
            }

        .side-navbar .close-btn {
            position: absolute;
            top: 15px;
            right: 25px;
            font-size: 30px;
            color: #ffffff;
        }

    .open-btn {
        font-size: 24px;
        cursor: pointer;
        background-color: #143963;
        color: #ffffff;
        border: none;
        padding: 15px 25px;
        border-radius: 25px;
        position: fixed;
        top: 20px;
        left: 20px;
        z-index: 101;
        transition: background-color 0.3s ease;
    }

        .open-btn:hover {
            background-color: #0d2a4a;
        }

</style>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        var progressFill = document.querySelector('.progress-fill');
        // Add the class to start the animation
        setTimeout(function () {
            progressFill.classList.add('loaded');
        }, 100); // Delay to ensure DOM is fully loaded
    });

    function openNav() {
        document.getElementById("sideNavbar").style.width = "250px";
    }

    function closeNav() {
        document.getElementById("sideNavbar").style.width = "0";
    }
</script>










